steps:

* name: 'gcr.io/cloud-builders/docker'
  args:

  * 'build'
  * '-t'
  * 'gcr.io/$PROJECT_ID/devops-practice:$COMMIT_SHA'
  * '.'

images:

* 'gcr.io/$PROJECT_ID/devops-practice:$COMMIT_SHA'

options:
logging: CLOUD_LOGGING_ONLY

